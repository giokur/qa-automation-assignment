name: Run specific type of tests

on:
  workflow_dispatch:
    inputs:
      test_type:
        type: choice
        description: Which type of tests
        required: true
        options:
          - ui
          - api
          - integration

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  api-tests:
    if: ${{ github.event.inputs.test_type == 'api' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run API tests
        run: |
          mvn clean test \
            -Dgroups="${{ github.event.inputs.test_type }}"

      - name: Generate Allure Report
        if: always()
        run: mvn allure:report

      - name: Upload Allure results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure report for Pages
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: target/site/allure-maven-plugin

  integration-tests:
    if: ${{ github.event.inputs.test_type == 'integration' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run Integration tests
        run: |
          mvn clean test \
            -Dgroups="${{ github.event.inputs.test_type }}"

      - name: Generate Allure Report
        if: always()
        run: mvn allure:report

      - name: Upload Allure results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure report for Pages
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: target/site/allure-maven-plugin

  selenium-tests:
    if: ${{ github.event.inputs.test_type == 'ui' }}
    runs-on: ubuntu-latest

    services:
      selenium:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444
        options: >-
          --shm-size="2g"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Run Selenium tests
        run: |
          mvn clean test \
            -Dselenide.remote=http://localhost:4444/ \
            -Dgroups="${{ github.event.inputs.test_type }}"

      - name: Generate Allure Report
        if: always()
        run: mvn allure:report

      - name: Upload Allure results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure report for Pages
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: target/site/allure-maven-plugin

  deploy-allure:
    needs: selenium-tests
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy Allure Report to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
